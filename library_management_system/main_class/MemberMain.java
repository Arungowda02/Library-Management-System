package main_class;

import entities.Book;
import implimentations.member_implimentation.MemberImpl;

import java.util.List;

import static static_variables.StaticVariables.*;

public class MemberMain {
    public static void main(String[] args) {
        MemberImpl member = new MemberImpl();
//        System.out.format("%-20s %-20s %-20s %-15s %-10s%n", "Title", "Author", "ISBN", "PublicationYear", "isAvailable"
//        +"\n------------------------------------------------------------------------------------------");
//        List<Book> availableBooks = booksDatabase.stream().filter(Book::isAvailable).toList();
//        availableBooks.forEach(System.out::println);

//        System.out.println("\nSelect the book serial number : ");
//        Book book = availableBooks.get(sc.nextInt()-1);
//        member.borrowBook(book);


//        member.returnBook(borrowedBooks.get(0));


//        System.out.println("\nListing all borrowed books.");
//        member.listBorrowedBooks().forEach(System.out::println);

    }
}
